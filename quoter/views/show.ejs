<%-include("./partials/head.ejs") %>
<body class= "m-auto " style="background-color: rgba(0, 0, 0, 0.9);">
  <%- include("./partials/nav.ejs") %>  
  <% if (user) { %>
    <h1 class="d-flex justify-content-center text-light " style="background-color: rgba(0, 0, 0, 0.6);"> Search Results</h1>
    <form method = 'POST' action = '/ranquotes' class="text-center">
    <button type = 'submit' class="btn btn-dark mx-auto">Add To My Quotes</button>
    </select>
    <% } %>
    <div class="container-fluild">  
      <div class="row d-flex justify-content-center">
      <% for (i of quote) { loop++%>
        <div class="card text-center m-2" style="width: 18rem;">
          <div class="card-body">
            <% if (user) { %>
            <input type='checkbox' name ="<%= loop %>" value="<%= loop %>" >
            <% } %>
              <h5 class="card-title"><%= i.fakeauth %></h5>
              <p class="card-text"><%= i.quote %></p>
            <% if (user) { %>
            </input>
            <% } %>
          </div>
        </div>
      <% } %>
      </div>
    </div>
    <% if (user) { %>
    <button type = 'submit' class="btn btn-dark">Add To My Quotes</button>
  </form class=" pb-2">
  <% } %>
<%- include("./partials/footer.ejs") %>
